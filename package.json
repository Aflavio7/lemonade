{
  "name": "email-lead-capture-monorepo",
  "version": "1.0.0",
  "description": "Email Lead Capture Automation Platform - Email to WhatsApp Lead Conversion",
  "scripts": {
    "start": "cd server && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "db:migrate": "cd server && npx prisma migrate dev --name init",
    "db:seed": "cd server && node prisma/seed.js",
    "setup": "npm run install:all && npm run db:migrate && npm run db:seed"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}